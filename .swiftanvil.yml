# SwiftAnvil project configuration
# See STYLE_GUIDE.md (style.guide) for the canonical style and SOLID rules.

lint:
  structure:
    # TODO: Refactor large files and split top-level types to tighten these thresholds.
    # Current values accommodate legacy files; target: max_lines 350, max_top_level_types 4.
    max_lines: 900
    max_top_level_types: 12
    mixed_type_kinds: 4
